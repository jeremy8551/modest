
1. -- 将 POM 中依赖的版本号统一放在 `modest/pom.xml` 中 properties 标签中
2. -- 补全所有注释
3. 补全所有单元测试，考虑：最大值、最小值、异常等场景
4. 查找 bug 与漏洞
5. -- 修改注解处理器，-Ascan.superTypes= -Ascan.annotations 参数设置的类如果不存在，就不执行注解处理器逻辑，不要跑异常，可以继续向下编译
6. 删除 mail、compress 模块；使用 Hutool 工具实现脚本引擎中的 zip unzip tar rar 等压缩与发送邮件的命令，适配不同版本的 Hutool：检查 Hutool 依赖的版本号，根据版本号选择不同的 Hutool 工具类实现，优先使用高版本的 Hutool 工具类实现；
7. 删除 build 模块
7. -- 增加 annotation 模块：将注解类从模块中提取出来，放到 annotation 模块中；
8. -- 注解处理器的参数长，简化配置
9. ioc 模块使用类扫描注解处理器实现
10. -- 使用字节码技术实现：JDK不同版本
11. --合并 core、tool、crypto、printer 模块；
12. --合并 modest-cn 到 modest-tool 模块
13. --开发类似于 Java 语言中的 for each 循环命令
14. -- 开发删除 Java 文件中所有行都有空白字符前缀的脚本方法
15. -- for 语法中指定 index 字段
16. -- for in 右侧的表达式，\`codegen\` 命令返回的结果
17. 使用 Hutool 提供的 poi 实现 codegen 命令
18. MailFactory Hutool 版本号与方言类之间的映射
19. -- 脚本引擎支持 +=
20. -- echo `命令右侧如果不是双引号，则执行命令，输出命令的输出`
21. -- 增加maven插件目标：将源代码目录写入 meta-inf
22. 扫描注解处理器的配置机制：使用类，还是使用文件？
